# ExercicioApiFactoryExceptions
Foi criado uma API para saques e depositos, utilizando o design patterns Factory e aplicado exceções utilizando o ExceptionHandler para aplicar validações que foram usadas através da dependencia @Validate nos atributos da API. 
